{
    "name": "Python",
    "build": {
        "dockerfile": "Dockerfile"
    },
    "postStartCommand": "pip install -r requirements.txt",
    "customizations": {
        "vscode": {
            "extensions": [
                // Habilita o botão de play para os scripts python.
                "ms-python.python"
            ]
        }
    }
}